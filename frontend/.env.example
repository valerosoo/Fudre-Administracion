# Copiar como .env.local para desarrollo, o setear al buildear para producción.
# Si no se define, el frontend usa la ruta relativa /api (funciona con el proxy de vite.config.ts
# o cuando el frontend y el backend PHP se sirven desde el mismo dominio).
VITE_API_URL=http://localhost:8000/api
